# Provider coverage

Network visualization now emits `provider_coverage.tsv` beside normalized edge/path tables. The
coverage report includes provider, evidence edge count, signed evidence edge count, unsigned or
unknown edge count, consensus participation count, reference count, and snapshot count.

This report is a coverage/provenance summary. It is not a provider quality ranking and does not
calibrate biological truth.
