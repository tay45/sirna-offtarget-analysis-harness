# Runtime Consensus Building

Consensus V2 edges are built from provider evidence, lineage groups, and evidence quality records.

Support counts are grouped by normalized entity pair and semantic relation. Positive, negative, unsigned, contextual, lineage, publication, original database, and access provider counts are stored separately. Opposing positive and negative support creates a conflicting consensus sign.
