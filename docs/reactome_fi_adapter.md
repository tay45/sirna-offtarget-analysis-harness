# Reactome FI Adapter

Reactome FI-only evidence is always unsigned, functional, and non-causal. FI can
support context or modules, but it cannot create a directional expectation unless
an independent signed source supports the same relation.

