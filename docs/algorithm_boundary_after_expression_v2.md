# Algorithm Boundary After Expression V2

This pass ends at validated expression input, trusted imported or approved normalized gene-level effect, committed typed V2 artifact, and read-only downstream access.

Deferred subsystems: short-read isoform interpretation, equal-transcript prior, transcript-set definition, N, M, M/N, intended-target calibration, expected direct effect, residual effect, secondary effects, and final classification.
