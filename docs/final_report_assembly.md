# Final Report Assembly

Final reporting assembles committed upstream artifacts. It does not rerun classification, pathway analysis, graph construction, SVG/PNG/GraphML generation, or scientific table generation.

The final assembly layer reads committed classification and network-visualization contracts, copies committed visualization artifacts into the final bundle, links stage reports and config history, and writes final HTML/JSON summaries.
