# Equal-Prior M/N Consistency

Under the equal-transcript prior, each eligible transcript has weight `1/N`. The
sum of qualifying transcript weights must equal the count-derived M/N ratio.

The verifier rejects disagreement between count-derived and weight-derived
fractions.
