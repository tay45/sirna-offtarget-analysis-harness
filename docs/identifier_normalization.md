# Identifier Normalization

Provider identifiers pass through a centralized normalization layer that records
input type, normalized identifier, output type, mapping status, ambiguity,
candidate mappings, provider, database version, snapshot id, organism, and
warnings. Unmapped and ambiguous records remain auditable.

