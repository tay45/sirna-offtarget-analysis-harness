# Pathway Network Visualization

Visualization consumes committed classification, mechanistic-network, and
enrichment contracts. GraphML includes node and edge attributes for sign,
providers, references, lineage groups, functional-only status, causal eligibility,
conflict, and path ids. PNG is omitted when no real renderer is available.

