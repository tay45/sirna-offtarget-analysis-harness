# Reporting Helpers

The current validated release exposes reporting helpers for staged run metadata,
artifact catalogs, provenance records, and optional plot manifests.

The package does not provide executable downstream classifier reports in the
current source tree. Users should inspect committed stage reports, provenance,
the transcript targetability evidence artifacts, and the terminal ratio tables.
