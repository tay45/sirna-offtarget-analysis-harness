# Pathway Verification V3

Current verification foundations include:

- annotation snapshot checksum verification
- `.verified` marker checks
- complete-membership local ORA tests
- V2 pathway payload artifact generation
- SVG/GraphML marker semantics tests
- bottleneck path-confidence tests

Full V3 semantic verification of every contract reference remains partial.
