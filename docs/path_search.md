# Path Search

Path search runs on consensus mechanistic edges. Signed paths compose all edge
signs. Unsigned, unknown, or conflicting edges prevent definitive directional
expectations but can be retained as context when configured.

