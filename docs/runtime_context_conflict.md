# Runtime Context Conflict

Contextual conflict records are emitted only when retained paths include opposing positive and negative signed support for the same candidate.

Missing decisive context remains unresolved context, not global conflict. The current runtime records strongest positive and negative paths by path confidence and warns when missing context prevents a global classification.
