# Pathway Provider Manifests

Each provider manifest includes provider, snapshot id, organism, endpoint,
request parameters, retrieval timestamp, database/API version when available,
normalization schema version, raw/normalized files, SHA256 checksums, record
counts, warning count, license/source notes, and completeness status.

