# Legacy Renderer Removal

The repository still contains deprecated candidate-summary rendering helpers for
legacy report compatibility. Production network visualization stages use typed
mechanistic and pathway-gene renderers.

Remaining work is to remove the compatibility report path completely after
downstream report consumers migrate to the V2 contract outputs.
